{
  "name": "text-signing-contract",
  "version": "0.1.0",
  "description": "Solana smart contract for text signing with payment",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "deploy": "anchor deploy",
    "frontend": "cd app && npm run dev",
    "install-frontend": "cd app && npm install"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
